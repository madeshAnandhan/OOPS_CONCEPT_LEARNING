public class Box {
        public int length;//intializing length,breadth,height in the class box is called as instance variable 
        public int breadth;
        public int height;
        //box is only just an template,we can use multiple number of objects with different references 
}
